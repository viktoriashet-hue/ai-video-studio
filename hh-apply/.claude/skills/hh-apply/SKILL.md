---
name: hh-apply
description: Полуавтоматические отклики на hh.ru через браузер с обязательным подтверждением пользователя на каждый отклик. Вызывать командой /hh-apply с аргументами: должность, город, лимит откликов.
allowed-tools: ["Read", "Edit", "mcp__claude-in-chrome__*"]
---

# hh-apply

Ты помогаешь пользователю откликаться на вакансии hh.ru через браузер
(Chrome-интеграция Claude Code — НЕ API, соискательский API hh.ru
закрыт с 15.12.2025).

## Порядок действий

1. Прочитай profile.md в корне проекта — единственный источник
   правды об опыте пользователя. Не выдумывай факты сверх него.

2. Разбери аргументы вызова: должность, город, лимит откликов
   (по умолчанию 10, если не указан).

3. Через браузер открой hh.ru, выполни поиск по должности и городу.
   Затем вызови логику скилла hh-vacancy-filter, чтобы отобрать и
   упорядочить вакансии по свежести публикации: сначала все вакансии
   за последние 24 часа, и только когда они закончатся — вакансии от
   24 часов до 3 суток. Вакансии старше 3 суток в работу не брать.

4. Для каждой вакансии по очереди — строго в порядке, который вернул
   hh-vacancy-filter (сначала группа "24 часа", потом группа
   "3 суток"):
   a. Открой её, прочитай требования и обязанности полностью.
   b. Проверь требуемый уровень английского в тексте вакансии
      (поле "Знание языков" и упоминания в требованиях/обязанностях —
      "английский", "English", "intermediate", "upper-intermediate",
      "advanced", "fluent", "свободный", "разговорный" и т.п.).
      Уровень пользователя — A2 (profile.md, раздел "Языки"). Если
      вакансия требует уровень выше A2 — пропусти её сразу, кратко
      укажи причину ("требуется английский B1+"), НЕ вызывай
      hh-cover-letter и не считай вакансию использованным откликом
      из лимита. Если уровень английского в вакансии не указан или
      указан как A1/A2/базовый — переходи к шагу c.
   c. Вызови логику скилла hh-cover-letter, чтобы получить
      match_score и текст письма (или low_match без письма).
   d. Если match_score = low — пропусти, кратко объясни причину,
      не считай вакансию использованным откликом из лимита.
   e. Иначе покажи пользователю: вакансия, компания, найденные
      пересечения, текст письма. ОСТАНОВИСЬ и жди ответа —
      "отправляй" / "пропусти" / "перепиши иначе".
   f. Только при явном "отправляй" — вставь письмо в поле
      сопроводительного письма на странице и нажми кнопку отклика.

5. По достижении лимита откликов — остановись, спроси пользователя,
   продолжать ли с новым лимитом.

## Жёсткие правила

- Никогда не отправлять отклик без явного подтверждения в этой же
  сессии.
- Пауза между действиями в браузере — не кликать подряд без разбора.
- Если вёрстка hh.ru выглядит непривычно или элемент не находится —
  остановиться и сообщить пользователю, не пытаться "угадать" кликами.
- При капче или требовании логина — остановиться, попросить
  пользователя пройти это вручную.

## После сессии

Допиши в logs/sessions.md блок с датой, таблицей вакансий (статус:
отправлено/пропущено + причина) и итоговой сводкой. Не перезаписывай
существующее содержимое файла — только дописывай.
